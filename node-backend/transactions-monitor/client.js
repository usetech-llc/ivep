const Client = require("node-rest-client").Client;
const ClientConfig = require("./config.js").ClientConfig;

const restClient = new Client();

class Client {
    constructor(){
        config = ClientConfig;
    }
    getBTCBatch()
}

export default Client;
